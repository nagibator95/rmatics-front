import * as actions from './router.actions';
import * as selectors from './router.selectors';

export const RouterActions = actions;
export const RouterSelectors = selectors;

export * from './router-store.module';
